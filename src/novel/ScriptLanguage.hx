package novel;

/** This class defines the "movie" script API.
    These are the methods used to create the game.
**/
class ScriptLanguage {
    /** Select the default text font **/
    private function font(name:String) {
        
    }

    /** Register the root path for all game assets **/
    private function assets(path:String) {
        
    }

    /** Set the game's main title **/
    private function title(name:String) {
        
    }

    /** Set the player's default name **/
    private function playername(name:String) {
    }

    /** Register a character with the game **/
    private function character(id:String, pathspec:String) {
        
    }

    /** Start a new scene **/
    private function scene(name:String) {
        
    }

    /** Select a background for a scene **/
    private function background(path:String) {
        
    }

    /** Display a registered character's image given a tag **/
    private function show(tags:String) {
        
    }

    /** Apply an effect to a character or the scene as a whole **/
    private function effect(name:String) {
        
    }

    /** Display text in the text box for the player or another character **/
    private function text(text:String, ?name:String) {
        
    }
}
